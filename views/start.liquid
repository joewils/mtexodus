<!DOCTYPE html>
<html>
  {% include 'head' %}
  <body>
    <div class="container">
      {% include 'masthead' %}
      
      <div class="row">
        <div class="col-lg-12">
          {% if message %}
            <h2 class="danger">{{ message }}</h2>
          {% endif %}
          {% include 'upload' %}
      </div>
      
      {% include 'footer' %}
    </div> <!-- /container -->
  </body>
</html>